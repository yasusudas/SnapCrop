# SnapCrop

React + Vite で構築した、複数画像キュー対応の色基準一括切り抜きツールです。

## セットアップ

```bash
npm install
npm run dev
```

## ビルド

```bash
npm run build
```

## 主な機能

- 複数の `jpg` / `png` をキューとして読み込み
- 1枚目の画像でスポイト選択した基準色を全画像へ適用
- 上下左右の各方向ごとに切り抜き有効化
- 許容量スライダーで色一致の幅を調整
- 個別 PNG ダウンロード
- 一括 ZIP ダウンロード
